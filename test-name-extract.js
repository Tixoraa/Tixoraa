console.log("Testing firstName and lastName handling:"); const test = { name: "John Smith", firstName: null, lastName: null }; const extractedFirstName = test.firstName || (test.name ? test.name.split(" ")[0] : ""); const extractedLastName = test.lastName || (test.name && test.name.includes(" ") ? test.name.split(" ").slice(1).join(" ") : ""); console.log({ extractedFirstName, extractedLastName });
